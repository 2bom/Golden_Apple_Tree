/*
// 'function' is not same with 'function()'.
// 'function' literally means that function. 'function()' means the value, result of the function.

 ex) Fix bugs with filter(array, function);
 productsICanEat = _(filteredNuts).filter(function(noNutsProduct) {
 return _(noNutsProduct.ingredients).all(mushroomsFilter);
    });
*/



/*
// What the fucking [] is this?!

  [] == false // true
  [] == true // false

  var test = function (x) {
    if (x) {
        return true;
    } else {
        return false;
    }
  };

  test([]); // true

  [] == [] //false
  NaN == NaN // false
*/
