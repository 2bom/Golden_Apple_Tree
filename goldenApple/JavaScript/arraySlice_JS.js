/*
 // Array.prototype.slice();

 arr.slice(begin, end);

 method returns a `SALLOW COPY` of a portion of an array into a new array object.

 if begin === undefined, slice begins from index 0.
 If end is omitted(end === undefined), slice extracts through the end of the sequence (arr.length).
 'end' index is not including sliced array.

 ex1) Range of index : begin <= slicedArray < end

 ex2)
 var fruits = ['Banana', 'Orange', 'Lemon', 'Apple', 'Mango'];
 var citrus = fruits.slice(1, 3);

 // fruits contains ['Banana', 'Orange', 'Lemon', 'Apple', 'Mango']
 // citrus contains ['Orange','Lemon']

*/
